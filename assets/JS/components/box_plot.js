const e = React.createElement;

class BoxPlot extends React.Component {
  constructor(props) {
    super(props);
    this.state = {

    };
  }

  render() {

    return (
      <p></p>
    );
  }
}

// const domContainer = document.querySelector('#bar_chart');
// ReactDOM.render(e(BoxPlot), domContainer);
